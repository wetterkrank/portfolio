import { css } from "styled-components";

const variables = css`
  :root {
    --nav-full-height: 120px;
    --nav-reduced-height: 70px;
    --main-padding-x: 48px;
    --main-padding-x-sm: 24px;
    --wrapper-max-width: 1328px;
  }
`;

export default variables;
